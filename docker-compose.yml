version: "3.9"

services:
  karaoke-clipper:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./data:/app/data
    command: ["python", "-m", "app.main", "--help"]
